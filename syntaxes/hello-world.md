# Hello World in Go

Go を理解するために基本的なコードを見てみましょう。
以下のコードを`main.go`という名前で保存します：

```go
package main

import "fmt"

func main() {
    fmt.Println("Hello, World!")
}
```

実行方法：

```bash
go run main.go
```
